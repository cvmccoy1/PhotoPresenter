<Window x:Class="PhotoPresenter.Views.ExportProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export Favorites"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="HeadingText" Grid.Row="0" Text="Exporting favorites…"
                   FontWeight="SemiBold" Margin="0,0,0,10"/>
        <ProgressBar x:Name="ExportProgressBar" Grid.Row="1" Height="18"
                     Minimum="0" Maximum="100" Margin="0,0,0,8"/>
        <TextBlock x:Name="StatusText" Grid.Row="2"
                   TextTrimming="CharacterEllipsis" Margin="0,0,0,4"/>
        <TextBlock x:Name="CountText" Grid.Row="3"
                   Foreground="Gray" Margin="0,0,0,16"/>
        <Button x:Name="CloseButton" Grid.Row="4" Content="Close" Width="80"
                HorizontalAlignment="Right" IsCancel="True" IsEnabled="False"/>
    </Grid>
</Window>
