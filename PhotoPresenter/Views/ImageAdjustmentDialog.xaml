<Window x:Class="PhotoPresenter.Views.ImageAdjustmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adjust Brightness/Contrast"
        Width="360" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="Black" Height="180" Margin="0,0,0,12">
            <Image x:Name="PreviewImage" Stretch="Uniform"/>
        </Border>

        <TextBlock Grid.Row="1" Text="Brightness" Margin="0,0,0,2"/>
        <Slider x:Name="BrightnessSlider" Grid.Row="2"
                Minimum="-100" Maximum="100" Margin="0,0,0,12"
                ValueChanged="Slider_ValueChanged"/>

        <TextBlock Grid.Row="3" Text="Contrast" Margin="0,0,0,2"/>
        <Slider x:Name="ContrastSlider" Grid.Row="4"
                Minimum="-100" Maximum="100" Margin="0,0,0,4"
                ValueChanged="Slider_ValueChanged"/>

        <Grid Grid.Row="5" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Reset" Width="80" HorizontalAlignment="Left" Click="Reset_Click"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="OK" Width="80" Margin="0,0,8,0" IsDefault="True" Click="OK_Click"/>
                <Button Content="Cancel" Width="80" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
