<Window x:Class="PhotoPresenter.Views.ExportDeleteConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm Deletion"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="HeadingText" Grid.Row="0"
                   TextWrapping="Wrap" Margin="0,0,0,10"/>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                      MaxHeight="300" Margin="0,0,0,16">
            <ListBox x:Name="FileList" IsHitTestVisible="False"
                     BorderThickness="1" Padding="4"/>
        </ScrollViewer>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Delete &amp; Export" Width="120"
                    Click="DeleteAndExport_Click" Margin="0,0,8,0"/>
            <Button Content="Export Only" Width="90"
                    Click="ExportOnly_Click" Margin="0,0,8,0"/>
            <Button Content="Cancel" Width="75"
                    IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
